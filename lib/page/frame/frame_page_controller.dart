import 'package:get/get.dart';

class FramePageController extends GetxController {
  // var unreadCount = 0.obs;
  var selectPageIndex = 0.obs;
  // void updateUnread(int count) {
  //   unreadCount.value = count;
  // }
}
